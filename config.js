/* config.js — empty by default.
   Copy config.example.js over this file and fill in your key, OR
   use the in-app Settings page to configure (saves to localStorage). */
window.AZURE_TTS_CONFIG = window.AZURE_TTS_CONFIG || {
  key: "",
  region: "",
  voice: "fr-FR-DeniseNeural",
  rate: "-5%",
  pitch: "0%",
};
